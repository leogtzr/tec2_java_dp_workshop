package com.dp.strategy;

import com.dp.strategy.domain.TextFormatter;

public class StrategyApp {

    private static void showText(final TextFormatter formatter, final String input) {
        System.out.println(formatter.format(input));
    }

    public static void main(final String[] args) {
        // TODO: Fix this ...
        // showText();
    }
}
